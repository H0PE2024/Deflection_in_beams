function [ x ] = nPoint_dL( w, xStart, xEnd )
%NETPOINT_DISTRIBUTEDLOADS Finds net point of action of distributed loads
%   Takes input as the distributed load value and the range in which it
%   acts
    
    x = (xStart+xEnd)/2;  


end

